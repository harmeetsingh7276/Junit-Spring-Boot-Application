spring:
  application:
    name: JUNIT-BASIC
  datasource:
    url: jdbc:mysql://localhost:3306/person_schema
    username: root
    password: Prince@7276
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect:org.hibernate.dialect.MySQL8Dialect